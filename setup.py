from distutils.core import setup

setup(name='Smart Security',
      version='1.0',
      description='Module to avoid writing many similar decorators and attaching them to many methods',
      author='Piotr Domanski',
      author_email='piotrjerzydomanski@gmail.com',
      packages=['smart_security'],
      )
